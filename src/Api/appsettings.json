{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "//ConnectionStrings": "Set via env: ConnectionStrings__Postgres (key-value or postgres:// URI), or Db__Host/Port/Name/Username/Password. Local dev value lives in appsettings.Development.json.",
  "//Session": "Login sessions live in an HttpOnly cookie holding an opaque id; the server row is the authority. No secret to configure — the token is random per session. See docs/cookie-session-design.md.",
  "Session": {
    "LifetimeDays": 30,
    "RenewAfterHours": 24,
    "CookieName": "keepr_session"
  },
  "Storage": {
    "AccountId": "",
    "AccessKey": "",
    "SecretKey": "",
    "Bucket": "media",
    "ServiceUrl": "",
    "PresignExpiryMinutes": 15
  },
  "Quota": {
    "DefaultBytes": 5368709120
  },
  "//Sharing": "Public 'anyone with the link' file shares. PublicBaseUrl is REQUIRED: the absolute http(s) origin the share-link viewer is served from. Ships empty so every environment must set Sharing__PublicBaseUrl (dev: http://localhost:4200; prod: your public URL) — startup fails fast if it is missing or invalid, rather than building capability URLs from an untrusted request Host header. PublicAccessEnabled=false is the global kill-switch. See docs/shareable-links-design.md.",
  "Sharing": {
    "MaxExpiryDays": 90,
    "PublicBaseUrl": "",
    "PublicAccessEnabled": true
  },
  "//Registration": "Signups require this shared invite code. Set Registration__InviteCode as a SECRET env var in production. Empty fails closed: registration is refused entirely.",
  "Registration": {
    "InviteCode": ""
  },
  "//Admin": "Seeds the first admin at startup so the console is reachable (signups are invite-gated, so no account can grant itself the role). Ships blank — no admin is created. Set Admin__Email/Admin__Password as SECRET env vars in prod; the password is an initial secret, change it after first sign-in. See docs/admin-console-design.md.",
  "Admin": {
    "Email": "",
    "Password": ""
  },
  "Cleanup": {
    "IntervalMinutes": 30,
    "PendingMaxAgeHours": 24
  }
}
