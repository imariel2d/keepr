{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=media;Username=media;Password=media"
  },
  "//Registration": "Local-dev invite code. Production sets Registration__InviteCode as a secret.",
  "Registration": {
    "InviteCode": "keepr-dev"
  },
  "//Admin": "Seeds a dev admin on startup so /admin is reachable without a manual role promote. Local-only credentials — never a real secret. Sign in as admin@keepr.local / keepr-dev-admin.",
  "Admin": {
    "Email": "admin@keepr.local",
    "Password": "keepr-dev-admin"
  },
  "Storage": {
    "//": "Local dev points at MinIO (docker-compose). ServiceUrl overrides the R2 endpoint.",
    "AccessKey": "minioadmin",
    "SecretKey": "minioadmin",
    "Bucket": "media",
    "ServiceUrl": "http://localhost:9000"
  }
}
